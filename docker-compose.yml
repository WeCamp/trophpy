version: '2'

services:
  app:
    build:
      context:  ./docker/app
      dockerfile: Dockerfile
    image: trophpy:latest
    ports:
      - "80:8080"
    volumes:
      - .:/var/www/html
  mysql:
    image: mysql:5.7

